UPDATE CUSTOMER_ENROLLMENT
SET MOBILE_NUMBER1 = "1" || LPAD(TO_CHAR(CUSTOMER_ID), 9, '0') where customer_enrollment_id>10090006
 
UPDATE cms_card.CUSTOMER_ENROLLMENT
   SET email = CUSTOMER_FIRST_NAME || CBS_CUSTOMER_ID || '@gmail.com' where customer_enrollment_id>10090006
 
